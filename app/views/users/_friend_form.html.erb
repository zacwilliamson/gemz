<% unless current_user == @user %>
  <div>
  <% if current_user.friends_with?(@user) || current_user.pending_friends.include?(@user) %>
    <%= render 'unfriend' %>
  <% else %>
    <%= render 'add_friend' %>
  <% end %>
  </div>
<% end %>